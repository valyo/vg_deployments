# Server inventory
# Sensitive values (IPs, users) are in host_vars/*/vault.yml

[servers:children]
zfs_server
infra_server
backup_server

[zfs_server]
burkeng

[infra_server]
infra

[backup_server]
backup
