---
- include_tasks: server_bootstrap.yaml
- include_tasks: add_user.yaml

# TODO
#  - name: import zpools
  # zpool import ****pool -f
  # if possible, check if zsf and zpool are fine
#  - name: Configure Docker with the zfs storage driver
