---
services:
  app:
    container_name: sh_portal
    image: ghcr.io/valyo/sh-portal:${IMAGE_VERSION}
    user: "${SH_PORTAL_UID}:${SH_PORTAL_GID}"
    restart: unless-stopped
    ports:
      - "${PORT:-8087}:5000"
    volumes:
      # Persist SQLite database
      - ./data/instance:/app/instance
      # Persist generated invoices
      - ./data/invoices:/app/invoices
      # Persist generated certificates
      - ./data/certificates:/app/certificates
      # Google Sheets credentials (if needed)
      - ./data/credentials:/app/credentials:ro
    env_file:
      - .env
    environment:
      # Mail configuration - set these in .env or override here
      - MAIL_SERVER=${MAIL_SERVER:-smtp.gmail.com}
      - MAIL_PORT=${MAIL_PORT:-587}
      - MAIL_USE_TLS=${MAIL_USE_TLS:-True}
      - MAIL_USE_SSL=${MAIL_USE_SSL:-False}
      - MAIL_USERNAME=${MAIL_USERNAME}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
      - MAIL_DEFAULT_SENDER=${MAIL_DEFAULT_SENDER}
      # Google Sheets credentials path (inside container)
      - GOOGLE_APPLICATION_CREDENTIALS=${GOOGLE_APPLICATION_CREDENTIALS}
      - GOOGLE_SHEET_ID=${GOOGLE_SHEET_ID}
      - OAUTH_REDIRECT_URI=${OAUTH_REDIRECT_URI}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s


  mailcatcher:
    container_name: sh_portal_mailcatcher
    image: sj26/mailcatcher:${MAILCATCHER_IMAGE_VERSION}
    ports:
      - "${MAILCATCHER_SMTP_PORT}:1025"  # SMTP port
      - "${MAILCATCHER_WEB_PORT}:1080"  # Web interface
